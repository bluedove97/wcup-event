# CLAUDE.md — 홈페이지 제작 마스터 가이드

> **이 파일의 목적:** "홈페이지 만들어줘"라고 하면 처음부터 끝까지 순서대로 진행한다.
> 
> Claude Code가 전체 프로세스를 관리하고, 사용자가 직접 해야 할 부분은 상세히 안내한다.

---


## 🚀 시작하기

사용자가 홈페이지 제작을 요청하면, 아래 **5개 Phase**를 순서대로 진행한다.

```
Phase 1: 기획 (인터뷰)
    ↓
Phase 2: 프론트엔드 개발
    ↓
Phase 3: 백엔드 연동
    ↓
Phase 4: 관리자 페이지 (선택)
```

**각 Phase 완료 후 사용자에게 확인받고 다음 Phase로 진행한다.**

---

# Phase 1: 기획 (인터뷰)


## 개발 언어 및 규칙

- NextJS 사용
- React는 19버전이상
- typescript를 사용
- 정적인 html파일, css, 이미지는 public 폴더에서 서비스 처리

### 폴더구조

폴더 구조는 아래와 같은 트리구조 형식을 따르도록 해
현재 구조상 필요하지 않는 컴포넌트 폴더까지 만들 필요는 없어.
필요한 폴더와 파일은 알아서 생성해서 처리해.

```
my-app/
├── src/
│   ├── app/                # App Router (라우팅, 레이아웃, 페이지)
│   │   ├── api/            # API Route 핸들러
│   │   ├── dashboard/
│   │   ├── layout.tsx
│   │   └── page.tsx
│   ├── components/         # 재사용 가능한 컴포넌트 (UI, 공통 등)
│   │   ├── common/         # Button, Input, Modal 등
│   │   └── layout/         # Header, Footer, Sidebar 등
│   ├── hooks/              # 커스텀 훅
│   ├── lib/                # 외부 라이브러리 설정 (prisma, axios 등)
│   ├── types/              # TypeScript 타입 정의
│   ├── utils/              # 유틸리티 함수
│   └── styles/             # 전역 스타일 (tailwind, css)
├── public/                 # 정적 파일 (이미지, 폰트 등)
├── next.config.js
├── tailwind.config.js
├── tsconfig.json
├── package.json
└── README.md
```
---

## Claude Code가 할 일

1. **AskUserQuestionTool**로 인터뷰 진행
2. 사이트 유형 파악 후 맞춤 질문

## 첫 번째 질문: 사이트 유형 파악

```
어떤 유형의 웹사이트를 만드시나요?
답변: 월드컵 이벤트 베팅 웹사이트

```


---

## 이벤트 웹사이트 인터뷰

| 영역 | 확인 사항 |
|------|----------|
| **1. 타겟 & 목적** | 누구를 위한, 어떤 목적의 사이트인지 |
| **2. 필요한 페이지** | 메인, 베팅하기, 베팅조회 등 |
| **3. 참고할만한 사이트** | 기존 사이트 혹은 참고할 만한 사이트 |
| **4. 핵심 기능** | 회사소개, 주요서비스, 프로젝트 사례 등 |
| **5. 디자인 방향** | 참조 이미지, 색상, 톤앤매너 |
| **6. 백엔드 필요 여부** | 베팅 저장, 베팅 조회 |


---

## 인터뷰 규칙 (공통)

- **AskUserQuestionTool** 사용
- 한 번에 **최대 2~3개 질문**
- 이미 정한 내용을 **다시 묻지 말 것**
- "모르겠다" 또는 "알아서 해줘" → **합리적 기본값 제안 후 확인**
- **인터뷰 종료** 시점: 모든 필수 영역 완료 또는 사용자가 "인터뷰 종료"

**→ 사용자 확인 후 Phase 2로 진행**

---



# Phase 2: 프론트엔드 개발

## Claude Code가 할 일

1. 로고 있으면 헤더에 적용
2. NextJS 기반의 HTML/CSS 코드 작성
3. 반응형 디자인 적용
4. 버튼 링크 연결 (베팅하기 → 베팅 조회 페이지)




## 참조 이미지 규칙

- **참조 있음**: 정확히 일치시킬 것. 개선/추가 금지.
- **참조 없음**: 아래 가드레일 따라 고퀄리티 디자인

## 안티-제너릭 가드레일

| 항목 | ❌ 금지 | ✅ 해야 할 것 |
|------|--------|--------------|
| **색상** | Tailwind 기본색 (indigo-500, blue-600) | 커스텀 브랜드 색상 |
| **그림자** | `shadow-md` 단일 사용 | 색상 틴트된 레이어드 그림자 |
| **폰트** | 제목/본문 같은 폰트 | 디스플레이 + 산세리프 조합 |
| **애니메이션** | `transition-all` | `transform`, `opacity`만 |
| **인터랙션** | hover 상태 없음 | hover, focus, active 전부 |

## 버튼 상태 디자인 (필수)

모든 버튼에 아래 상태를 구현할 것:

```css
/* 기본 상태 */
.btn { background: #브랜드색상; }

/* 마우스 올림 */
.btn:hover { background: #진한색상; transform: scale(1.02); }

/* 포커스 (키보드 접근성) */
.btn:focus { outline: none; ring: 2px; ring-color: #브랜드색상; }

/* 클릭 중 */
.btn:active { transform: scale(0.98); }

/* 비활성화 */
.btn:disabled { opacity: 0.5; cursor: not-allowed; }
```

## 버튼 링크 연결

```html
<!-- 신청하기 버튼 → 결제 페이지로 이동 -->
<a href="https://결제페이지URL.com" target="_blank">
  <button class="btn-primary">지금 바로 신청하기</button>
</a>

<!-- 무료 상담 버튼 → 페이지 내 폼으로 스크롤 -->
<a href="#consultation-form">
  <button class="btn-secondary">문의하기</button>
</a>
```


## 출력 기본값

- 단일 `index.html`, 스타일 인라인
- Tailwind CSS CDN: `<script src="https://cdn.tailwindcss.com"></script>`
- 플레이스홀더 이미지: `https://placehold.co/너비x높이`
- 모바일 퍼스트 반응형



## Phase 2 완료 체크

```
□ 모든 페이지 NextJS기반의 HTML/CSS 완성
□ 반응형 디자인 적용
□ 버튼 상태 (hover, focus, active, disabled) 구현
□ 스크린샷 비교 검증 완료 (최소 2회)
```

**→ 사용자 확인 후 Phase 3로 진행**

---


# Phase 3: 백엔드 개발

> **백엔드가 필요 없으면 Phase 4로 건너뛴다.**

---

## 왜 백엔드가 필요한가?

```
[백엔드 없이]
사용자가 문의 폼 작성 → 제출 → ❌ 아무 일도 안 일어남

[백엔드 있으면]
사용자가 문의 폼 작성 → 제출 → ✅ 데이터 저장 → ✅ 관리자에게 알림
                             → ✅ 관리자 페이지에서 확인 가능
```

---
### Claude Code가 할 일

1. 백엔드는 NextJS의 api route로 작성
2. 데이터베이스는 Postgresql 사용. 이미 설치된 데이터베이스 접속정보가 있음
3. 사용할 테이블 명세 제공
4. 게임별로 유저베팅 → DB 저장 연동
5. 게임 조회 페이지에서 데이터 조회 연동


### Database 접속정보
 - DB종류: postgresql 17
 - host: 192.168.219.184
 - port: 5432
 - database: pidb
 - user: pipg
 - password: pipg12#$

### 테이블 명세
```
tb_game
+----------------------+
| game_id (PK)         |
| cont                 |
| create_by            |
+----------------------+
          |
          | 1
          |
          | N
          |
tb_betting
+----------------------+
| id (PK)             |
| sso_login_id        |
| game_id             |
| betting             |
| create_by           |
+----------------------+
```


### ⚠️ 보안 규칙

- ❌ API 키를 코드에 직접 입력 금지
- ❌ .env 파일을 GitHub에 커밋 금지
- ✅ .gitignore에 .env 추가 필수

## Phase 3 완료 체크

```
□ 테이블 생성 SQL 실행 완료
□ 게시글 제출 → DB 저장 테스트 성공
□ 사용자 페이지에서 게시글 내역 확인 가능
□ .env.local 생성, .gitignore 확인
```

**→ 사용자 확인 후 Phase 4로 진행 (필요 시)**

---

# Phase 4: 관리자 페이지 (선택)

> **필요 없으면 건너뛴다.**

## 🤖 Claude Code가 할 일

1. 관리자 페이지 생성
2. 비밀번호 보호 구현
3. 데이터베이스 연동 (상담 신청 목록 조회)

## 관리자 페이지 구성

| 탭 | 기능 |
|-----|------|
| **문의 관리** | 베팅 목록 확인, 상태 변경 (대기중 → 완료) |

## 상담 관리 화면 예시

```
┌─────────────────────────────────────────────────────────┐
│  📋 게임별 베팅 관리                                   │
├─────────────────────────────────────────────────────────┤
│  게임ID | sso_login_id  | 베팅상태   | 접수일   | 상세 │
│  ─────────────────────────────────────────────────────  │
│  1    | kim@email.com   | W | 2026.06.10 | [보기] │
│  1    | lee@email.com   | D | 2026.06.11 | [보기] │
│  1    | park@email.com  | L | 2026.06.11 | [보기] │
└─────────────────────────────────────────────────────────┘
```

## 보안 필수

```
□ 비밀번호 보호 적용
□ 추측 어려운 경로 (/dashboard-x7k2 등)
□ 세션 타임아웃 설정
```

## Phase 4 완료 체크

```
□ 관리자 페이지 코드 완성
□ 비밀번호 보호 동작 확인
□ 상담 관리에서 상담 신청 목록 조회 성공
□ 상태 변경 기능 동작 확인
□ 테스트 완료
```

---

# 🔒 보안 체크리스트

| 영역 | 항목 |
|------|------|
| **인증** | AuthN/AuthZ 분리, 최소 권한 |
| **공격 방어** | CORS, CSRF, XSS, SQLi |
| **통신** | HTTPS, 보안 헤더, 쿠키 보안 |
| **운영** | Rate Limit, Secret 관리, Audit Log |

---

# ⚠️ 엄격한 규칙

## 절대 금지

- 인터뷰 없이 코드 작성 시작
- API 키를 코드에 직접 입력
- 관리자 페이지 비밀번호 없이 배포
- `transition-all` 사용
- Tailwind 기본 blue/indigo 메인 색상
- 버튼에 hover/focus/active 상태 없이 배포

## 반드시 할 것

- **AskUserQuestionTool**로 인터뷰 진행
- 각 Phase 완료 후 사용자 확인
- 사용자가 할 일은 상세 가이드 제공
- 환경변수는 .env 파일에만 저장
- 모든 버튼에 상태별 스타일 적용

---

# 🎯 전체 프로세스 요약

```
Phase 1: 기획 ─────────────────────────────────────┐
  └── AskUserQuestionTool로 인터뷰                  │
  └── 회사 랜딩페이지: 제품/타겟/오퍼/버튼링크/상담폼 │
        ↓                                          │
Phase 2: 프론트엔드 개발 ──────────────────────────────┤
  └── HTML/CSS, 버튼 상태, 폼 UI                    │
  └── 베팅하기 → 베팅 조회 페이지                   │
        ↓                                          │
Phase 3: 백엔드 연동 ─────────────────────────────────┤
  └── 베팅 데이터 저장                            │  🤖 Claude Code
  └── 베팅 데이터 조회                             │  👤 사용자 협업
        ↓                                          │
Phase 4: 관리자 페이지 (선택) ─────────────────────┘
  └── 게임별 베팅 목록 확인 & 관리

✅ 완료!
```
